name: ecom-auth-prod

services:
  postgres-auth:
    image: postgres:16-alpine
    container_name: ecom-postgres-auth
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASS}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    volumes:
      - ecom_auth_data:/var/lib/postgresql/data
    ports:
      - "25431:5432"
    networks:
      - ecom-auth-int

  ms-auth:
    build: .
    container_name: ecom-ms-auth
    restart: unless-stopped
    depends_on:
      postgres-auth:
        condition: service_healthy
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE}
      CONFIG_SERVER_URL: ${CONFIG_SERVER_URL}
      DB_HOST: ecom-postgres-auth
      DB_PORT: 5432
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASS: ${DB_PASS}
    volumes:
      - ./logs:/app/logs
    networks:
      - ecom-auth-int
      - ecom-prod-net

volumes:
  ecom_auth_data:

networks:
  ecom-prod-net:
    external: true
    name: ecom-prod-net
  ecom-auth-int:
    name: ecom-auth-int
